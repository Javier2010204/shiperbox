<nav class="navbar navbar-expand-lg navbar-light white">
  <%= link_to 'ShiperBox', root_path, class: 'navbar-brand' %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
         <% if user_signed_in? %>
          <%= link_to 'cerrar sesion', destroy_user_session_path, method: :delete, class: 'nav-link' %>
          
          <% unless current_user.profile.nil? %>
            <%= link_to 'Mi perfil', profile_path(current_user.profile.id) , class: 'nav-link' %>
          <% end %>
        <% else %>
          <%= link_to 'Iniciar Sesion', new_user_session_path, class: 'nav-link' %>
        <% end %>
      </li>
      <li class="nav-item">
      
      </li>
    </ul>
  </div>
</nav>
