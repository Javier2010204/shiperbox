<% if current_user.profile.nil?%>
    <div class="container h-100">
        <div class="row d-flex align-items-center justify-content-center card p-5">
            <div class="col-xs-12 col-sm-10 col-md-8">
                <h2>Activa tu cuenta ShiperBox para empezar a utilizar la dirección en tus nuevas compras</h2>
                <p>Llena los datos del formulario para empezar a utilizar la direccion, guardar y monitorear tus pedidos</p>
                <p></p>
                <%= link_to 'Llenar datos del perfil ahora', new_profile_path, class:'btn btn-lg btn-info' %>  
            </div>
        </div>
    </div>
<% else %>
    <div class="container text-center">
        <div class="row d-flex align-items-center">
            <div class="col-xs-12 col-sm-10 col-md-10">
                <div class="top-space">
                    <h1 class="title-text">Mis Pedidos ShiperBox</h1>
                </div>
                
                <% if @buys.nil? %>
                    <p>Espera a que se te asigne un paquete</p>
                <% else %>
                    <%= render @buys%>
                <% end %>
                <%= link_to 'Agregar nueva compra', new_guide_path %>
                <br>
                <%= link_to 'cotizar', new_guide_path %>
                <br>
                <%= link_to 'rastrear paquete', new_guide_path %>
            </div>
        </div>
    </div>
<% end %>

